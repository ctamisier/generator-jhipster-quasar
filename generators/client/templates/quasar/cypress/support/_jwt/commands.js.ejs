Cypress.Commands.add('login', onLogin => {
  cy.request('POST', '/api/authenticate', { username: 'admin', password: 'admin', rememberMe: false }).then(response => {
    sessionStorage.setItem('jhi-authenticationToken', response.body.id_token);
    if (onLogin) {
      onLogin();
    }
  });
});
